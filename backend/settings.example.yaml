$schema: "./settings.schema.yaml"
api_settings:
  db_url: "postgresql+asyncpg://postgres:postgres@localhost:5432/postgres"
  session_secret_key: ""  # use `openssl rand -hex 32` to generate a random key
